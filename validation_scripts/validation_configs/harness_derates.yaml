shared:
  kernel_launch_overhead_s: 6.e-06

device_types:
  A100_PCIe:
    dram_util: 0.70
    network_util: 0.85
    compute_util: 0.60

  A100_SXM4:
    # Backend-calibrated (extended_roofline GEMM model REQUIRED via
    # RAPID_GEMM_BACKEND=extended_roofline): residual compute 1.00,
    # merged mem/net utilization 0.75. Calib 10.59 / holdout 10.61 (2026-07-07).
    dram_util: 0.75
    network_util: 0.75
    compute_util: 1.0

  H100_SXM5:
    # Backend-calibrated (extended_roofline GEMM model REQUIRED via
    # RAPID_GEMM_BACKEND=extended_roofline): residual compute 0.85,
    # merged mem/net utilization 0.85. Calib 7.92 / holdout 6.45 (2026-07-07).
    dram_util: 0.85
    network_util: 0.85
    compute_util: 0.85