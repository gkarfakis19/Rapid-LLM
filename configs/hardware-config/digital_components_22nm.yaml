# THE MEASURED DIGITAL SYNTHESIS LIBRARY (22nm) — D32.
#
# PROVENANCE. Every block below is COPIED VERBATIM from OPTIMA's
# auto-generated synthesis library:
#
#   source file    /app/nanocad/projects/cim_ctt_big_optima/perf_model/configs/digital_hw_components_22nm.yaml
#   source reports /app/nanocad/projects/cim_ctt_big_optima/rtl/reports_22nm_1000_efh
#   generated by   OPTIMA's parse_rtl.py (a synthesis-report parser)
#   copied         2026-08-24, by hand, value for value
#
# The numbers are MEASURED VIA SYNTHESIS at 22nm: area from the post-synthesis
# area report, power from the post-synthesis power report, at the frequency and
# pipeline depth each block was synthesised for. They are not estimates and they
# are not scaled.
#
# WHY A COPY AND NOT A REFERENCE (D21, D32). cim_ctt_big_optima is another
# project on a shared mount. A run of this repo must never depend on it being
# present, readable, or unchanged, so the library is CHECKED IN here and the
# path above is provenance, not a dependency. Nothing in this repo fetches the
# source file at runtime.
#
# WHAT IS NOT COPIED. OPTIMA's collections multiply each composition by an
# `overhead` fraction (0.0 to 0.4 depending on the collection; the
# largest is the depthwise conv at 0.4) for glue and
# routing. D28 forbids safety margins, so this repo composes at overhead 0 and
# says so rather than carrying an uncalibrated pad. OPTIMA's area_scale /
# power_scale / dvfs_scale knobs are likewise not copied: they retarget the
# library to another node, and a retargeted number is no longer a measured one.
#
# WHAT IS NOT CALIBRATED, IN THE SOURCE'S OWN WORDS. Two blocks carry caveats
# that the numbers alone do not show, and both are named here so a reader of a
# composed area knows what it rests on:
#   * M_REG — the source's description reads "Register for MAMBA BF16.
#     TODO!!!!!". The area and power below are copied verbatim from the
#     synthesis report, but the SOURCE flags the block as unfinished. M_REG is
#     one of the three units in every derived vector lane (115.163 of 2829.418
#     um2, 4.1%) and is instantiated once per pool lane on every macro, so a
#     revision of it moves both the engine and the pool.
#   * TRANSPOSER — OPTIMA instantiates 9 per systolic replica and its own
#     comment says the 9 is unexplained. This repo composes 1 and says so in
#     the chiplet disclosure.
#
# BLOCKS THIS REPO NEVER COMPOSES. Copied for completeness, used by nothing:
# COMBINED_CONV, COMBINED_CONV_SEQUENTIAL and
# INT8_BUFF_DIV_SYS_ARRAY_PENALTY (measured at zero area and zero power in the
# source, so they are inert by measurement); M_SOFTPLUS and
# ELASTIC_BUFFER_256_MXFP4_ELEMS (real measured blocks that no composition in
# this repo instantiates — the scan lane holds no transcendental unit because
# the scan work law counts no transcendental op, and no elastic buffering is
# modelled between stages). Their absence from a composed area is a FLOOR, not
# a measured zero.
#
# HOW IT IS READ. cim_timing.SynthesisLibrary.load("22nm") parses this file.
# A block this file does not name is REFUSED BY NAME — there is no default
# block, no interpolation and no zero-fill (ADJ-4, no invented numbers).

technology: "22nm"
source: "/app/nanocad/projects/cim_ctt_big_optima/perf_model/configs/digital_hw_components_22nm.yaml"
source_reports: "/app/nanocad/projects/cim_ctt_big_optima/rtl/reports_22nm_1000_efh"
generated_by: "OPTIMA parse_rtl.py"
copied: "2026-08-24"
measurement: "measured via synthesis, 22nm"

blocks:
  FP_COMP:
    area_um2: 75.19
    power_W: 3.913e-05
    frequency_GHz: 1.0
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "FP comparison unit from fpnew"
  FP_ADD:
    area_um2: 1417.41
    power_W: 0.00200231
    frequency_GHz: 1.0
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "FP add/sub unit from fpnew (FMA configured for add/sub)"
  FP_MULT:
    area_um2: 1296.845
    power_W: 0.00214539
    frequency_GHz: 1.0
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "FP multiplication unit from fpnew (FMA mult-only)"
  BF16_EXP:
    area_um2: 999.544
    power_W: 0.0011236
    frequency_GHz: 1.0
    pipeline_depth: 4
    provenance: "measured via synthesis, 22nm"
    description: "BF16 exponential unit from FloPoCo with 4-cycle pipeline"
  M_SOFTPLUS:
    area_um2: 999.544
    power_W: 0.0011236
    frequency_GHz: 1.0
    pipeline_depth: 4
    provenance: "measured via synthesis, 22nm"
    description: "BF16 exponential unit from FloPoCo with 4-cycle pipeline. USED AS PROXY FOR SOFTPLUS"
  BF16_RECIP:
    area_um2: 3472.065
    power_W: 0.00886468
    frequency_GHz: 1.0
    pipeline_depth: 3
    provenance: "measured via synthesis, 22nm"
    description: "Custom BF16 reciprocal unit with Newton-Raphson, 3-cycle pipeline"
  GEMMINI_SYS_ARRAY:
    area_um2: 767330.073
    power_W: 0.92093
    frequency_GHz: 1.0
    pipeline_depth: 1
    rows: 32
    cols: 32
    provenance: "measured via synthesis, 22nm"
    description: "Gemmini systolic array 32x32 for matrix operations"
  TRANSPOSER:
    area_um2: 10606.661
    power_W: 0.0111271
    frequency_GHz: 1.0
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "Matrix transposer for K matrix preparation"
  M_REG:
    area_um2: 115.163
    power_W: 0.00012228
    frequency_GHz: 1
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "Register for MAMBA BF16. TODO!!!!!"
  ELASTIC_BUFFER_256_MXFP4_ELEMS:
    area_um2: 8125.064
    power_W: 0.00713462
    frequency_GHz: 1
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "Elastic buffer for 256 MXFP4 elements (272 x 4 bits)"
  COMBINED_CONV:
    area_um2: 0
    power_W: 0.0
    frequency_GHz: 1.0
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "Combined div+conv block for row-based format conversion"
  COMBINED_CONV_SEQUENTIAL:
    area_um2: 0
    power_W: 0.0
    frequency_GHz: 1.0
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "Combined div+conv sequential block for column-based format conversion"
  INT8_BUFF_DIV_SYS_ARRAY_PENALTY:
    area_um2: 0
    power_W: 0
    frequency_GHz: 1
    pipeline_depth: 1
    provenance: "measured via synthesis, 22nm"
    description: "Penalty per PE of array to enable MX exec"
