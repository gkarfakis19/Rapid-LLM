# Bug ledger — evidence-backed quirk classification (restructure P1)

Produced by an evidence-first audit against `git show 85894c6:*` (the pre-rewrite originals) and the
live code at `03ace0e`. Classes: **A** confirmed bug (fixing SHOULD change output) · **B** deliberate
modeling choice (preserve) · **C** semantically inert (ordering/naming; may perturb AstraSim wall
clock via node-id priority) · **D** unclear, needs an experiment or an owner call.

> **Scope note added 2026-07-29 (P5 verification).** The reframe below is about the ZeRO-3 **per-rank
> gather** machinery at `par_degree > 1`, and it scopes items 1, 4, 5(10a) and 10f. It does **not**
> cover **A6**: the S8/S15 cross-stage prefetch anchor fires at exactly `tp=cp=1, pp=2`, which every
> zero2/zero3 spec is, and both zero3 goldens pin it.

> **Reframe that removes four constraints:** the ZeRO-3 per-rank gather machinery is **dead in the
> golden matrix**. `equiv/configs.py:163-174` builds zero2/zero3 specs only at `dp=2, tp=1, cp=1`, so
> `par_degree == 1` and `_should_shard_zero3_transformer` (`program/pipeline_fine.py:307`) always
> returns False. Items 1, 4, 5(10a) and 10f are **unvalidated code, not pinned behavior** — no golden
> constrains them. Treat that region as new development with new goldens.



## Perf pass 4, 2026-08-02 ("try harder")

| item | what |
|---|---|
| **R3 window certificate** | The same-device-restricted boundary walk LANDED — with the unprovable global no-round-trip assumption replaced by a PER-QUERY CERTIFICATE. Every cross-device dep edge that exists before R3 is recorded at creation with its slot and phase class (fwd / bwd by the producing work's direction; stage-0-delta MoE cross-rank edges as "other"). A boundary window containing cross edges of ONE phase class only (and no MoE edge) is stage-monotone: cross edges all step the same way, so an off-device ancestor cone can never contain a node of the query's device, and pruning off-device preds is EXACT. Mixed windows (the fwd/bwd turnaround) and MoE windows take the exact full walk. RECOMPUTE cross transfers are classified fwd though they run in the bwd phase — that can only make a window read MIXED and fall back, never mis-certify. Gated twice: the oracle test replays every matrix query against the unbounded walk, and `test_r3_window_certificate_branches_are_both_exercised` pins that BOTH branches fire on the matrix (a silently-disabled certificate is slow-but-correct; a silently over-certifying one is the dangerous direction — either changes no output, so only a branch census catches them). 175B `_apply_r3`: 2.0 s → **1.1 s**; 1T in the commit message. |
| **V7 always-on** | `build(check_group_membership=True)` is the DEFAULT. V7 — every member device of a communicator issues the group's collectives — is the strongest static deadlock check in the tree (a violated group is a silent AstraSim hang), costs one O(collectives) pass, and its opt-in rationale (legacy-lowered per-device clone programs) was deleted with the lowering. Already measured clean over 192 configurations when A2 was fixed; the flip was flagged then as "a separate, now-unblocked decision" and is now taken. |

## Perf pass 3, 2026-08-02 ("as much as you can")

Output-preserving throughout (gate 221/221 bit-identical per item; suite 1311).

| item | what |
|---|---|
| **proto→IR fusion** | The `_Proto*` mirror classes and `_finish`'s second construction are DELETED — the ops the builder makes ARE the final IR instances. During the build, `uid` holds the dense construction nid, `deps` (and a transfer's `consumers`) are mutable lists, and a collective's `label` is None; `_finish` renumbers uids/deps/producers in place, retypes the lists to tuples and interns labels in uid order (so the `name_N` suffixes are byte-identical). Builder-only facts the IR deliberately does not carry (P5 deleted ordering metadata from ops) live in side arrays indexed by nid: `_orders`, `_succs`, `_entry_names`. The `int()` truncation of transfer sizes moved from `_finish` to construction (same value). Also halves peak build memory — no 13M dead protos at GPT 1T. |
| **shared chain steps** | The GEMM `ComputeStep`s of a memo-instantiated chain are shared BY REFERENCE across microbatches: they carry the `f"{entry}_{direction}"` name PREFIX and the per-instance `_{work_name}_rank{r}` tail lives on the chain (`ExpandedChain.name_suffix`), composed at materialization for exactly the steps with `entry_name` set. Zero per-instance step construction. |
| **V6 device-restricted probe** | `has_path` first walks only nodes on the pair's own device (a found path is real, so True is SOUND — and the path that makes a V6 pair safe is almost always the device's own serialization spine); only a probe miss falls back to the exact unrestricted walk (MoE cold→hot joins, turnaround windows). |

Measured, GPT 175B analytical: build(FLAT) 29.3 → **24.3 s**, whole run 41.9 →
**37.8 s** — **parity with the pre-branch tree (36.1 s), while running V1-V9
validation the legacy flattener never did**, and 5x faster (6.9 s) when
`estimate_memory: false`. GPT 1T in the commit message.

## Perf pass 2, 2026-08-02 ("this work is procedural")

Output-preserving throughout (gate 221/221 bit-identical per item; suite 1311).

| item | what |
|---|---|
| **R5 producers: no walk** | "Does a producer's exit reach the optimizer's entry" is **R3's own postcondition**: R3 processed every adjacent pair of the device's projection (`Schedule.implied_deps`' contract) and either found it implied or added the SCHEDULE edge, so the device's slots form a connected serialization spine and any same-device node at an earlier slot reaches any later one through it. A producer needs an R5 edge only when its slot is AFTER the optimizer's (zero cases under GPipe; exact forward-cone fallback kept). R5b keeps the exact per-reducer query in the small-cone direction (the reducer's descendants — a sink's cone is empty). The three ancestor-set helpers are deleted. Also: the producer list was re-SORTED per optimizer chain (512 × sorted(131k) at 1T = ~50 s); one sort + one stage-partition now. GPT 1T `_apply_r5`: **151.7 s → ~1 s**; 175B: 2.6 s → 0.11 s. |
| **R3 boundary verdict** | `_r3_implied`: budgeted backward probe (decides implied-via-short-path in a few pops), then the source's forward cone bounded by `slot(target)`. Both prune the same D2 window, so the truth value is identical — asserted by `test_r3_implied_answers_exactly_what_the_unbounded_walk_answers`, which replays every matrix query against the unbounded walk. **At 1T this bought little (116 → 110 s): both cones are window-sized there.** The next design is on record: within one boundary window a path `exit(m,d) → entry(m+1,d)` arguably cannot round-trip through another device (forward transfers leave d, backward transfers re-enter it only far outside the window), which would make a SAME-DEVICE-restricted walk exact and O(chain) — needs the round-trip impossibility argued precisely (small-pp turnaround is the suspect case) before it can land, with the oracle test as the harness. |
| **validation fused** | V1-V5 + V8 are ONE pass over ops; per-GROUP facts verified once per interned GroupKey on the first referencing op. **V6 (group races) is now ~all of validation's cost** (~2.5 s of 175B's 5.5, ~most of 1T's 149) — per consecutive same-group pair `has_path` over the uid window. Next lever, same shape as R3's. |
| **remaining 1T shape** | 641 s pre-sort-hoist: `_finish` 214 (proto→IR double construction — the fusion design is inventoried: 7 construction sites, side arrays for order/succs/entry_name, in-place renumber), `validate` 149 (V6), `_apply_r3` 110 (window walks), `_expand_all` 104 (1.6M chain materializations), replay 55, overlap 44. |

## Optimal-perf pass, 2026-08-02 (second session — "fix all of this")

All output-preserving: **44/44 goldens bit-identical after every step** (gate run
between each item). Measured on GPT 175B analytical (pp=8 L=96 mb=64 tp=8).

| item | what |
|---|---|
| **template-expansion memo** | A LAYER/RECOMPUTE expansion is a pure function of `(kind, direction, layer)` — the microbatch appears nowhere but in the GEMM step names. `BlockExpander` computes the structure once per key and re-instantiates per microbatch: `CommStep`s and JOIN steps (name-stable) are shared BY REFERENCE, GEMM `ComputeStep`s are rebuilt with the instance's name, and the `ExpandedChain` replica bypasses `__post_init__` (the cached chain already passed index/dep validation; indices are copied verbatim). 36,864 template expansions → 384. |
| **integer-rank Kahn** | `_kahn`'s heap carries a precomputed integer rank (one Timsort over the static, unique `_OrderKey`s) instead of the nested-tuple key, and indegree/uid live in dense lists. Popped sequence provably identical (`rank(a)<rank(b) ⟺ order(a)<order(b)`). |
| **slots=True** | `_Proto*`, `ComputeOp`/`CollectiveOp`/`TransferOp`, `ComputeStep`/`CommStep`/`StepRef`. |
| **WorkItem cached `__hash__`** | The dataclass-generated hash re-hashed the five-field tuple (two Python-level enum hashes) on every call — 4.35M hashes / ~13M enum hashes per build. Cached value is `hash()` of the SAME tuple, so dict iteration orders are unchanged. |
| **`_materialize_chain` fused** | Per-WORK invariants (schedule slot, device position, IR direction, MoE flag, kind role) hoisted out of the 1.2M-step loop; `_add_compute_step` deleted into it. |
| **`group_for` / `_pricing_axis` memos** | One call per collective instance, a few hundred distinct answers; interned `GroupKey`s also make every downstream dict hash the same object. |
| **memory replay** | Dependency countdown instead of `all(dep in done)` re-scans, per-op byte/kind/direction tables instead of closure chains per event, ready-list rebuild instead of copy+O(n) removes. Fires at the same times in the same sorted-successor order — peak bit-identical. |
| **`pipeline_style_recompute` DELETED** | `train_timing.py:297` hardwired it to `bool(full_recomputation)` with no config path, so the predicate's `(flattened_mode OR pipeline_style_recompute)` disjunct was true whenever the outer conjunct was, in every reachable state. The flag, the `RunPolicy` field, the `REQUIRED_MISC_KEYS` entry and the granularity coupling in `recompute_policy_for` are gone; RECOMPUTE items exist per (run, model), never per backend. Audit C3 is thereby closed as "never was a coupling". |
| **`sw_param.estimate_memory` wired** | The lever existed but read `self.sw_config` — an attribute that does not exist on the TimeCalculation object (the config lives on `hw_config`) — so it ALWAYS answered the default. Now a real `SWConfig` field with YAML parsing and a contract test. |

**Measured**: `build(FLAT)` 53.8 s → **31.9 s**; whole 175B analytical run 68.9 s
→ **44.6 s** (pre-branch flattener tree: 36.1 s); time-only (`estimate_memory:
false`) **6.9 s**. GPT 1T time-only **34.5 s**; full-with-memory run completes
(it previously did not finish in 50 min under contention) — numbers in the
commit message.

## Owner-directed fix pass, 2026-08-02

Owner: *"fix any OBVIOUS and clearly solvable issues."* Three landed, each cleared
by an internal CONTRADICTION rather than by appeal to a reference implementation,
plus the granularity rename (`COARSE` -> `PIPELINE`, `FINE` -> `FLAT`) and one
performance fix. **10 of 44 goldens recaptured; every other spec bit-identical.**

| item | verdict |
|---|---|
| **A6** | ✅ **FIXED.** Rows S8/S15 chose the ZeRO-3 prefetch anchor by layer arithmetic (`layer -+ prefetch_depth`); at a stage boundary that names a layer on the OTHER device, and `PARALLEL_TO`'s `deps(req) += deps(anchor)` (with `_resolve_anchor` falling back to every chain when none is local) made the gather inherit the foreign stage's predecessor set. **Contradiction:** the gather is a dp-axis collective over `stage(target)`'s replicas — no rank of another stage participates and no other stage produces its input — so a dependency on a stage-0 op is not a data dependency in any sense. `ZeRO3._prefetch_attach` walks the prefetch window deepest-first for a same-stage anchor and falls back to `BEFORE(target)`, which is row **S1**'s existing shape: the gather takes the layer's deps (the inbound cross-stage transfer) and the layer waits for it — *a stage issues its entry layer's gather when it becomes active*. **Delta: 2 goldens** — `analytical` **-3.89 %** (the cross-stage p2p SEND no longer waits on the next stage's parameter gather), `flattened` **+4.04 %** on the max rank and **-4.20 %** on stage 0's ranks (stage 1 loses a prefetch it could not physically have issued). 16 one-byte control SEND/RECV pairs disappear from the flattened bundle: they existed only to carry the foreign anchor's edge. **The `_ROOT_COMM_IS_UNTIMED` blocker did not materialize** — the fallback fires only at a stage boundary and a stage's entry layer always has the inbound transfer as a dep — and that is asserted, not argued (`test_no_zero3_gather_becomes_an_untimed_root`). Pinned by three tests in `test_build.py`, two verified to fail with `_prefetch_attach` reverted. |
| **activation sharding (`tp_sp ∧ tp>1 ∧ cp>1`)** | ✅ **FIXED, and the ledger's diagnosis was half wrong.** `_sequence_parallel_degree` is a PRODUCT now (`tp` when sp shards, `cp` when context shards, `tp*cp` when both), so it can name two axes. **But the predicted "memory peak moves on four goldens" did NOT happen and could not have**: `memory_estimation.py`'s sequence-sharded residual branch is gated on `mode.endswith("tensor_sequence")`, which is false at the hybrid, so the memory census never consults this function there. Memory peaks are bit-identical. What actually moved is `get_residual_f/b`, which divides the residual by `seq_degree` with no compensation. **Delta: 4 goldens** (`dp2tp2cp2pp2sp1`), analytical **-0.29 %**, hybrid -0.20 %, hierarchical -0.16 %, flattened -0.067 %. The "does the hybrid deserve the SP residual layout in the memory census" question is untouched and still open — it is a modeling judgment, not a fall-through. |
| **layernorm f/b disagreed by `cp`** ⚠ NEW | ✅ **FIXED.** Found while fixing the above. `get_layernorm_f`'s `else` branch charged the FULL sequence; `get_layernorm_b` has always charged `seq/seq_degree`. They agree everywhere `seq_degree == 1` and differ by exactly `cp` under `ParallelismMode.CONTEXT`. One tensor, one forward, one backward, two element counts — a contradiction, and the backward is the right one (each cp rank holds `seq/cp` tokens). Both are now the single expression `batch * ceil(seq_len / seq_degree) * d_model`, which also absorbs the HYBRID branch's second `/ self.tp` (a compensation for the fall-through above; at `tp_sp` the element count is unchanged either way, and at `tp_sp`-OFF the old code claimed a tp split that plain TP does not do — no golden covers that cell). **Delta: 4 goldens** (`dp1tp1cp2pp2`), all four modes **-0.16 % to -0.19 %**, compute-µs -0.20 %. |
| **R5's ancestor walk** | ✅ **FIXED (performance, output byte-identical).** `_apply_r5` computed the optimizer's FULL ancestor set once per optimizer INSTANCE — i.e. once per device — and the optimizer is the last op of its stage, so that set is essentially the whole upstream program: `O(devices x E)`. Under GPipe the rule then adds ZERO edges, so the whole walk was a redundancy proof. `_ancestors_covering(target, wanted)` stops as soon as every candidate source has been seen (exact: it exits early only when nothing is left to find, and runs to exhaustion — the edge-adding path — otherwise). Also, both ancestor walks now test membership BEFORE pushing rather than after popping, which at in-degree ~6.6 was 6.6x the list churn. **GPT 175B `build(FLAT)`: `_apply_r5` 16.4 s -> 2.8 s, whole run 68.9 s -> 54.1 s.** All 44 goldens unchanged. |

## Architectural-residue pass, 2026-07-28

| item | verdict |
|---|---|
| **AxisSpec registry** | ✅ **BUILT** (`program/axes.py`). Every axis fact — canonical order, the CLUSTER/PIPELINE/REPLICA split, coordinate strides, "layout must declare this axis", per-granularity device axes, activation sharding, the dp-companion set — is one `AxisSpec` row. Hand-written axis literals in `placement/layout/groups`: **56 -> 4**. `cluster_coords` lost its per-axis `tp_size=/cp_size=/ep_size=/pp_size=` signature for one `sizes` mapping and its three `if "<axis>" in axis_order` blocks for one `cluster_strides` loop. `tests/test_axes.py` includes an AST guard that FAILS on any new hand-written axis tuple in `program/` — it immediately caught two I had missed (`workload.ParallelDegrees`, `policies/routing.AxisRouting`). **The payoff beyond brevity:** BUG_LEDGER 19 is now the query `shards_work and replicates_parameters`, and `work._DP_COMPANION_AXES` is its RESULT rather than a hand-kept list. |
| **19 residual (`dp == 1, cp > 1`)** | ✅ **CLOSED** — and NOT by materializing cp at PIPELINE, which would have redefined PIPELINE (one op per stage -> `cp` ops) and moved every PIPELINE golden. The actual blocker was that emission DROPPED a one-member dp communicator, dangling R5b's `optimizer -> reducer` edge. `et_emit` now emits a zero-duration no-op for that shape **when something depends on it**, and still drops it when it is a sink (the legacy Step-10 skip, stated precisely: deleting a sink cannot orphan anything). The analytical evaluator prices it from `participants` as before. The gates then move to the reduction GROUP: `sharding_policy_for` uses `dp * _reduction_companion_degree(degrees)`, derived from `program.axes`. **Delta: 4 of 44 goldens, all `dp1cp2`** — analytical **+30.14 %**, flattened +3.39 %, hybrid +1.35 %, hierarchical +0.00 %. Those rows previously had NO gradient reduction at all. |
| **BLOCK sharding** | ✅ **LATENT GAP FOUND + FIXED while closing the above.** `policies_for` relied on an ACCIDENT — its own docstring said "No granularity-specific SHARDING override is needed: a BLOCK workload is single-replica and `sharding_policy_for` already answers NullSharding at `dp <= 1`". Once the test became `dp * cp`, `cp > 1` broke the accident and dp reducers appeared INSIDE the block program, which exists only to MEASURE one layer for retiming — measured as **+1192 us** on the retimed transformer duration of `train:hierarchical:dp2tp2cp2pp2mb2sp1`. BLOCK now selects `NullSharding` explicitly. |
| **"Two memory models"** | ❌ **THE PREMISE IS WRONG AT HEAD.** `program/memory_sim.py` does NO sharding arithmetic — it is a pure replay over per-op deltas from `memory_estimation.build_memory_data`. One census, one replay, cleanly separated. The real question was which of `_sequence_parallel_degree` (RESIDENCY) and `Placement.activation_shard_size` (TRANSPORT) is authoritative; they disagree in 6 of 12 `(tp, cp, tp_sp)` cells. `tests/test_activation_sharding_contract.py` now pins every cell with its reason: 4 agree, 4 are the DELIBERATE plain-TP split (10c — resident whole, sent split by `--scatter-gather-tensors-in-pipeline`), and **exactly 2 are a real gap** (`tp_sp and tp > 1 and cp > 1`, where the residual is sharded on BOTH axes but the `elif` can only name one, so memory is over-reported by `tp`). Left alone: it moves the memory peak on four goldens and is an owner call, not a cleanup — but it can no longer be mistaken for intent. |
| **FLAT memory build on the timing path** | ⚠️ **MY OWN CHARACTERIZATION WAS WRONG.** It is not "a number `tot_time` never reads": the local `training_peak_gb` is unused, but `simulate_peak` sets `memory_peak_gb` / capacity / headroom and writes the memory-summary artifacts, and memory peaks are T1-gated. It is a SECOND OUTPUT, not waste. What was true is that it dominates wall clock in the PIPELINE-emitting modes (~5 min vs ~23 s for the coarse build on GPT 1T) because nothing else builds a FLAT program there. Now separable: `TimeCalculationLLM.estimate_memory` (default True = unchanged behavior) lets a caller that only wants a time skip it. |

## Adjudication pass, 2026-07-28 (owner rule: bug vs. intended)

Owner rule applied throughout: *when a finding could be functionality intended
under a set of internally consistent assumptions, defer to that reading; fix
only clear bugs.* A finding is cleared as a CLEAR BUG only by an internal
CONTRADICTION — some other part of the model committing to the opposite
assumption — never by appeal to what a reference implementation does.

| id | verdict | evidence |
|---|---|---|
| **19** | ✅ **CLEAR BUG — FIXED** | `_shard_gemm_descriptor` under `ParallelismMode.CONTEXT` sets `shard_m = ceil(m / cp)` for EVERY gemm type (QKV/FFN1/FFN2/OUT_PROJ), so each cp rank's weight gradient covers `1/cp` of the tokens, while `layer_params_per_rank` divides by `tp` only, so parameters are REPLICATED across cp. Partial gradients, replicated parameters, and the only cp-axis collectives in the model (`train_timing.py` `COMMUNICATION_RULES`) are ring-attention ACTIVATION collectives reducing `dL/dx`, not `dL/dW`. Contradiction, so not a consistent assumption. **Fix:** the reduction group is `dp x cp`. `interconnect_type` declares `"dp*cp"` at `cp>1`; `SyncRequirement.is_dp` becomes `DP_AXIS in axes`; `et_emit._dp_group_devices` folds each device with its cp siblings and `_intern_dp_group` interns the communicator BY MEMBER SET (a `min(sibling)+1` id would have collided with the per-device dp stage ids the pure-dp ZeRO gathers still use); `_pricing_axis` keeps the analytical cost keyed on `dp` because `"cp+dp"` is not a key in the interconnect table. **Delta: 3 of 44 goldens move** — `analytical dp2tp2cp2pp2` **+10.81 %** (a 4-rank all-reduce prices above a 2-rank one), `flattened` **-1.01 %**, `hybrid` **-0.59 %** (AstraSim sees half as many distinct dp collectives contending). Emitted BYTES are unchanged (6,081,740,800 on the flattened row, relabelled `dp` -> `cp+dp`): the fix corrects communicator MEMBERSHIP, not volume, and traffic proportional to cp is correct once the partials are actually summed. Every `cp == 1` spec is bit-identical. Pinned by `test_gradient_reducer_spans_cp_because_cp_gradients_are_partial`, `test_dp_cp_reducer_emits_one_communicator_per_cp_sibling_set` and `test_cp1_is_byte_identical_after_the_dp_cp_change`, all verified to fail with `is_dp` reverted. |
| **19 residual** | ⚠ OPEN, scoped | At `dp == 1, cp > 1` there is still NO gradient sync: `sharding_policy_for` returns `NullSharding` at `dp <= 1`. Widening that test to `dp * cp` was TRIED and reverted — at PIPELINE the device space is `("pp","dp")`, cp has no device extent, the cp-only group has one member, emission correctly skips it, and R5b's optimizer dep is then left dangling (`EmissionError: op 'optimizer_stage0' depends on 'embedding_grad_b0' which has no ET node`). Needs PIPELINE to materialize cp, or a cp-only reducer that is analytically priced and emission-elided. |
| **15** | ✅ **CLEAR BUG — FIXED** | Endpoint dp gradient BYTES were not per-rank and not tied-aware, and carried an activation-shaped `+ seq*hidden*batch` term. Now route through `_embedding_softmax_params_per_rank`, the same census as the apply-grad price (12) and the memory census. **Delta: 12 of 44 goldens, ALL DOWN**, median -5.5 %, max -27.9 %, every one a `dp=2` row. |
| **R3 / R5** | ✅ **CLEAR BUG (performance) — FIXED** | Two quadratic passes introduced by the rewrite. `_apply_r3`'s `_reaches` walked the whole ancestor set per microbatch boundary; `Schedule.implied_deps` already promised the monotone slot sequence it needed. `_apply_r5` recomputed the optimizer's ancestor set once PER PRODUCER. On GPT 1T (`pp=64 L=128 mb=512`, 330,816 ops): R3 hours -> 3.9 s, R5 427 s -> 10.1 s, `build()` 441 s -> 23.4 s. Output byte-identical (golden 221/221 with the modeling fixes stashed). New invariant **D2** is CHECKED in `_apply_r3`, not assumed. |
| **1(ep)** | ❌ **STALE — already fixed by 18** | The entry's premise ("`_param_stats_per_rank` never divides by ep") is false since the census consolidation: `_moe_ffn_param_components_per_rank` computes `experts_per_rank = moe_num_experts / _moe_routing_group() + n_shared_experts`, and `_moe_routing_group()` returns `ep` in training. |
| **17** | ❌ **NOT A BUG — resolved as intended** | Modeling ZeRO-1 as "DDP + sharded optimizer MEMORY" is internally consistent. At `zero_stage == 1` this codebase uses ALL_REDUCE, so dividing apply-grad by `dp` would have each rank update `1/dp` of the parameters with NO collective to share the result — incoherent. Making it right means giving ZeRO-1 the RS+AG pair, i.e. ZeRO-2's collective set. |
| **A4** | ❌ **DORMANT — precondition does not occur** | Measured `num_stages_initial == len(devices)` and zero colliding ranks on every buildable config (zero3 pp2 dp2, zero3 tp2cp2 pp2 dp2, base pp2 dp2). |
| **A5** | ❌ **GONE** | `_ensure_zero3_per_rank_edges` was deleted with `pipeline_fine.py`; only docstring citations remain. |
| **A6** | ❌ **LEFT — consistent reading** | Measured: the layer-`L` gather anchors on the cross-stage TRANSFER, not on a foreign-device op. "A stage issues its parameter gather when it becomes active" is a coherent model of a non-prefetching implementation. It contradicts `prefetch_depth`'s intent but not the model's arithmetic. |
| **16b / 16c** | ❌ **LEFT** | Both are explicitly pinned-not-endorsed with a plausible physical reading each (a GEMM epilogue out of fast memory; a ring hop's chunk). Each moves every GEMM / every collective and needs its own review. |
| **20** | ✅ **CONFIRMED, CORRECTED, ANNOTATED** | The ledger said 5 H100 configs; scanning all 96 parseable configs finds **10** — `A100_SXM4_80GB_case-{C,D,E,F,G}` as well. Nothing is mispriced (`roofline` takes a max), so the fix is the comment the entry asked for, now in all 10 files. |
| **two memory models** | ❌ **LEFT — modeling judgment** | Real: `TENSOR_CONTEXT_HYBRID` appears NOWHERE in `memory_estimation.py`, whose sharding branch is gated on `mode.endswith("tensor_sequence")` (`tp_sp and tp>1 and cp==1`). But 10c already ruled the residency-vs-transport split deliberate on the plain-TP side, so which leg is wrong is a judgment, not a contradiction. |

## A-list (confirmed bugs, ranked by impact)

> **A1 is fixed (restructure W1 / P0).** `cache_key = sha256("df-astra-cache/2|" ‖ workload_sig ‖ et_sig)`
> where `workload_sig` is the old formula (manifest + system + network + remote memory + comm groups)
> and `et_sig` is a name-bound digest of every emitted `llm_graph.<rank>.et`
> (`astrasim_lib/integration.py::_hash_workload_files`). Two workloads with equal op multisets but
> different dependencies, node-id priority order, p2p tags or rank assignment no longer collide —
> pinned by `tests/test_astrasim_cache_key.py`. `workload_sig` survives as the stable per-bundle RUN
> IDENTITY in the new `astra_runs.json` result log (written in every cache mode), which is what let
> the golden recapture stay a strict superset. Two enabling fixes shipped with it: `run_perf.py` now
> HONORS an inherited `RAPID_ASTRA_CACHE_MODE` (its map keys used spaces, so the declared `NO_CACHE`
> silently resolved to `CACHE_READWRITE` and *overrode* every caller — the golden gate's
> `RAPID_ASTRA_CACHE_MODE=NO_CACHE` never reached the subprocess), and `equiv/runner.py` now forces
> `NO_CACHE` explicitly instead of `CACHE_READWRITE`. No modeling output changed.

| id | Bug | Site | Correct behavior · expected delta |
|---|---|---|---|
| **A1** ✅ FIXED (W1) | **AstraSim result-cache key omits all DAG structure.** `cache_key = sha256(manifest ‖ system ‖ network ‖ remote_mem ‖ comm_groups)`; the `.et` files are never hashed, and the manifest is a *sorted multiset* with no deps, no order, no p2p peer, no tag. | `astrasim_lib/integration.py:456-466`, `_hash_file_bundle:140-153`; manifest `program/et_emit.py:560-606` | Two workloads with equal op multisets but different dependency structure / priority order / p2p pairing **collide** and a stale wall time is returned silently. Hash the emitted ET bytes (or add deps+peers+order to the manifest). **No model change; makes staleness loud.** ⚠ This masks exactly the kind of change the restructure makes — fix FIRST. |
| **A2** ✅ FIXED (final wave) | **DP collectives attached to `rank_tails[0]` only.** One cluster rank bears the whole grad all-reduce/reduce-scatter; the other `par_degree-1` ranks emit nothing. | `program/pipeline_fine.py:616-629`, placement at `:829-849`; now `program/policies/sharding.py::_spread_for` | `_spread_for` returns `SyncSpread.PER_CLUSTER_RANK` **unconditionally** (it used to read `CommSpec.tp_shard`, which `train_timing.py:4495` sets on `zero3_transformer_gather` alone). Placement/group construction needed no change: B4 already resolves `PER_CLUSTER_RANK` against `cluster_devices(stage_of(place_on))` and B2 already returns `group=None` per instance device for a dp requirement, so the `par_degree` dp communicators come out disjoint (`et_emit` interns one wire group per owning device). **Measured** (A/B on one tree, `tp*cp*ep = 4`): `bytes_by_axis[dp]` **×4.000** on both flattened tp>1 specs; the dp communicators carrying traffic go **2 of 8 → 8 of 8**, each with an identical op/byte load; every non-dp group, every `compute_micros` and every memory peak **bit-identical**. `train:flattened:dp2tp2cp2pp2mb2sp1` **+188.1 %** (0.04489122 → 0.12935004 s), `train:flattened:dp2tp2cp1pp2mb2sp1:moe:ep2` **+184.2 %**; all PIPELINE-emitted rows (analytical/hybrid/hierarchical) unchanged to the last digit. |
| **A3** | **tp-overlap head drops `is_moe_layer` → memory DOUBLE-COUNT** (not misclassification): tail files layer L under moe, head files the same L under dense, and the census sums both. | `program/pipeline_fine.py:186-196` (`OVERLAP_NODE_COPY_ATTRS`), census `program/memory_sim.py:352-368`, `:412`/`:432` | Add `is_moe_layer` to the overlap copy list. Reported peak/static memory **decreases** by one dense layer's worth per split MoE layer per device; can flip capacity warnings. **Timing unaffected.** Path is live (`llm_execution.py:776-787` passes MoE templates *and* tp_overlap). |
| **A4** | **Collective-only stage rank collision.** `rank = dp_idx*num_stages_initial + stage_idx` with a pre-extension count: `rank(first_ext, dp=0) == rank(devices[0], dp=1)`. | `program/legacy_lowering.py:327`, `:604-613`; `program/ir.py:235-242` | Asserted verbatim today in `tests/test_program_ir.py:490-493`. At dp≥2 the collective is silently appended to another replica's trace. Correct: use the post-extension device count, or refuse collectives on non-compute devices. **Dormant — no reachable config found** (all ZeRO-3 ±pp hops are one-stage between adjacent host/target pairs, and `program/layout.py:113-114` bounds-checks). |
| **A5** | **ZeRO-3 dead store with live side effects.** `per_rank_edges = self._ensure_zero3_per_rank_edges(...)` is assigned and never read (unlike the sibling at `:717-721`), yet it consumes `par_degree` op ids (shifting emission priority) and appends the discarded edges to real nodes' `.parents`. | `program/pipeline_fine.py:620-629` | Benign only *by accident* (the lowering's `_collect_objects` is children-only, so the orphans are never discovered). Also a clone-cache hazard: a gather reachable as both `dp_child` and `zero3_attachment` gets whichever expansion runs last. Delete or use. **Unreachable today.** |
| **A6** ✅ **FIXED 2026-08-02** (see the owner-directed pass above) | **The ZeRO-3 prefetch anchor is chosen by LAYER ARITHMETIC and lands on another device.** Rows **S8** (`_forward_layer`) and **S15** (`_backward_layer`) host layer `L`'s parameter gather on `layer_item(L - prefetch_depth)`. At a stage boundary that layer is on the OTHER pipeline stage, so a gather correctly PLACED on the device that consumes the parameters inherits the *foreign* stage's predecessor set (`build.py` `PARALLEL_TO`: `deps(req) += deps(anchor)`, and `_resolve_anchor` falls back to every chain when the anchor has none on the requirement's device, `:1200-1201`). The policy already KNOWS: `ZeRO3._via` returns `VIA_DATA_FLOW` instead of `VIA_ALL` for exactly these requirements. It narrows the SUCCESSOR side and leaves the DEP side to inherit blind. | `program/policies/sharding.py` `_forward_layer` (S8) `:594-618`, `_backward_layer` (S15) `:677-699`, `_via` `:449-457`; `program/build.py` `_resolve_anchor` `:1200-1201` | A layer-`L` parameter all-gather is a **dp-axis** collective over `stage(L)`'s dp replicas; no rank of another stage participates and no other stage produces its input, so the dependency on a stage-0 op is not a data dependency in any sense. It is the *prefetch-depth* heuristic ("issue the gather one layer early"), which is a **device-local** notion; at a stage boundary "one layer earlier" resolves to a layer on another device, where it means nothing. Real FSDP/ZeRO-3 + PP has no cross-stage signal: each stage prefetches against its own schedule and issues its first layer's gather at its own entry. **Correct fix:** re-anchor on the gather's own device (a `_dep_anchor` rule beside `_via`). **Expected delta:** moves `train:analytical:dp2tp1cp1pp2mb2sp0:zero3` (measured ≈ **-4.3%** with a crude local anchor) and needs `analytic_sim._ROOT_COMM_IS_UNTIMED` settled first, because a stage's first gather legitimately becomes a program ROOT and would otherwise be free. **Fixed 2026-08-02** as a deliberate modeling change with its own golden movement (measured -3.89 % analytical, not the -4.3 % a crude local anchor gave); `_ROOT_COMM_IS_UNTIMED` turned out not to be a blocker. P5 makes the two evaluators agree on the CURRENT anchor by putting the cross-rank edges on the wire (INTERFACES amendment 2026-07-29). |

## B — preserve (deliberate modeling choices)

| id | Item | Why it stays |
|---|---|---|
| 1 | ZeRO-3 per-rank gather bytes "un-divided" | Not a bug for tp/cp: each cluster rank owns its own TP shard (`params_per_layer_per_rank` is already `/tp`, `train_timing.py:783-787`) and gathers from the **dp** group. Total fine traffic `= cp·ep·P_layer` is correct. `int()` is a no-op. |
| 5 | `local_comp_time` zeroing | The assignment was already dead in legacy; the same quantity is modeled once per stage as the `optimizer` node. Attaching it per dp edge would double-count by `layers_per_stage`. |
| 8 | `_pipeline_interleave_scale` multiplies the whole total | Owner-scoped closed form (DESIGN §6). Quantified bias, both making totals **too low**: the additive DP tail is scaled by `s<1`, and interleaving's `v`× p2p hop count is ignored. Worst at small `mb`, large `pp`, large `v`, large tail share (e.g. `pp=8,mb=8,v=4` → `s=0.65`; a 20% tail is under-counted by ~7% of total). Under ZeRO-3 + large dp the tail is **not** small, so the docstring's "small comm share" bound does not hold there. |
| 9 | Same-stage zero-byte PIPELINE events | The *counter* consumption is inert (monotonic ⇒ relative order preserved), but their **presence** is load-bearing: they pop at the current timestamp and drive the ready-scan that admits successors. |
| 10d | Softmax pinned to cluster rank 0 | **Revisited with A2 (final wave) and it is NOT the same class — it stays B.** The premise "TP ranks would do this concurrently" assumes `linear_softmax_f/b` is already a per-TP-rank duration. It is not: `train_timing.get_linear_softmax_f` calls `single_gpu_gemm_forward` on the **unsharded** `gemm_shapes["linear"] = (batch, seq_len, d_model, vocab_size)` (`llm_util.py:633`) under the explicit comment *"Linear softmax is modeled as running on a single device within each TP group"* (`train_timing.py:2504-2506`); only the pointwise term is divided by `tp*cp`. The dp gradient bytes agree with that: `softmax_size = grad_comm * hidden*vocab` and `embedding_size = grad_comm * (vocab*hidden + seq*hidden*batch)`, neither divided by `tp` (`train_timing.py:4591-4592`) — unlike the transformer reducer, whose attention terms are `_attention_param_components_per_rank` (`/tp`). So (compute placement, duration, gradient bytes) is one **consistent** three-legged simplification; flipping only the placement leg would put the FULL unsharded vocab projection on all `par_degree` ranks, i.e. `par_degree`× the FLOPs, and would move the critical path — trading a known-wrong occupancy for a differently-wrong one. **A2 is different in kind**: nothing about it re-derives a per-instance quantity — the collectives it adds already have the right size, and each one reduces a shard no other rank owns. The real 10d fix is the commented-out `_tensor_parallelism_gemm_forward` path at `train_timing.py:2501-2504` (vocab-parallel softmax) plus `/tp` on `embedding_size`/`softmax_size`, i.e. a **train_timing** change, after which `PlacementPolicy(spread_by_kind={... SOFTMAX: PER_CLUSTER_RANK ...})` becomes the correct companion — and that seam already exists, with no edit inside `build.py`. **Consequence carried by A2 today:** the embedding/softmax dp reducers now exist on all `par_degree` ranks while their compute does not, so ranks 1..`par_degree-1` anchor on cluster rank 0's chain — 12 extra 1-byte control SEND/RECV pairs on `train:flattened:dp2tp2cp2pp2mb2sp1` (SEND/RECV 16,777,264 → 16,777,276 B, +0.00007 %), and their `par_degree`× byte rise is an aggregate the *byte values* do not yet shard. |
| 10g | Singleton-group no-op; 1-byte controls | Required AstraSim-side workarounds, correctly one-homed in `et_emit`. |
| 10h | `b == 0` means "last microbatch" | Backward walks microbatches in reverse. Semantics correct; comment confusing. |

## C — inert (ordering/naming only)

`3` manifest records ALL_REDUCE as `-1` (collision unreachable — `_collective_enum` raises on `None`;
`-1` only sorts it first) · `7` Step-11 pre-SCOTCH-remap iteration (**endpoints are correct**; the
remap never changes stage identity, only `send_seq`/`recv_seq` → node-id priority) · `10e` non-unique
op ids from overlap head splits · `10f` `_offset_stage_device` raises out-of-range where legacy
produced a phantom device (equivalent in range; docstring overclaims equivalence).

## D — needs an experiment or an owner call

> **State of the D-list, 2026-07-27 (second pass).** **10b** and **10c** are settled and out of the
> "needs an experiment" class: 10b is FIXED (optimizer duration priced over the stage's own layers),
> 10c's **ep half** is FIXED (the divisor is a shard count `tp·cp`, not the device count `tp·cp·ep`)
> and its **tp/cp half** is resolved **CORRECT** with the code left alone. Both are kept in this
> table rather than moved out, because their rows now carry the evidence that stops them being
> re-litigated.
>
> **10b(T5) is ANSWERED, and the answer was not the one the entry guessed.** The suspected culprit —
> "`apply_grad` charges ~2 full optimizer passes, one of them a separate `grad_clipping` sweep" — is
> **false, and backwards**. Measured: the clip term is **0.21 %** of the apply-grad term, because
> `grad_clipping` is the one full-parameter sweep in the model that does not pass
> `mem_level=num_levels-1`, so `roofline` priced it against **`mem_layer[0]`, the register file** —
> where it lands MEMORY-bound at ~134 TB/s (comp_int 0.5 vs the L0 inflection 2.32), NOT compute-bound;
> the 0.2115 % figure IS the memory-bound answer (item **16**, now FIXED: it ADDS 33.3 %, it does not
> remove a double count).
> never converted to per-rank, so at `tp=8` the apply-grad price and the dp gradient payload were
> **5.667×** too large. That is what 10b's `L`× scaling amplified into a visible T5 regression.
>
> **Fixed in this pass: 11 (+14), 12, 13, 16, 18.** Item 11's first attempt was DEFECTIVE and an
> adversarial audit caught it — see its row; the correction is the reason 14 shipped alongside
> rather than later. Still open: **1(ep)** (unreachable today), **A6**, **15**
> (`embedding_size`/`softmax_size` dp gradient BYTES are still not per-rank — the last disagreement
> with `_param_stats_per_rank`, entangled with class-B **10d**), **16b**/**16c** (the two remaining
> `mem_level` questions, pinned not endorsed), **17** (whether Megatron's distributed optimizer means
> apply-grad is `/dp` at `zero_stage == 1`) and **19** (no `cp`-axis gradient reduction).
>
> **Two claims this ledger made were WRONG and are corrected in place**, both caught by an
> adversarial audit rather than by me: the *provenance* of 13 (the half-finished conversion is
> `c5823bb` "MLA PT#1 (Megatron-equal)", not `173a765`) and the *physics* of 16 (`grad_clipping`
> landed memory-bound at register bandwidth, not compute-bound at peak FLOPs — the ledger's own
> 0.2115 % figure was the memory-bound answer and disproved its own label). Both CODE fixes stood.

| id | Question | Experiment |
|---|---|---|
| **13** ✅ NEW + FIXED (2026-07-27) — the biggest of the three, and the cause of 10b(T5) | **The gradient / apply-grad path never counted PER-RANK parameters for the dense MLP.** `get_data_parallel_reduction_llm`, `get_data_parallel_reduction_sizes` and the `ep_dense_sync_bytes_dense` block all ran their attention terms through `_attention_param_components_per_rank` (which divides by `tp`) and then wrote the MLP inline as `ffn1_dim * d` / `intermediate_size * d` — the **FULL unsharded matrix**, charged on every one of the `tp` ranks. | **Not an interpretation: the file already contains the answer, twice.** (a) `_param_stats_per_rank`, the authoritative per-rank census, computes `intermediate_size * ffn_proj_factor * hidden_dim / tp` (`train_timing.py:783-785`); (b) the MoE branch of the very same helpers divides (`expert_param_size / tp` under `tp_ep`). One tensor, two answers, differing by exactly `tp`. **Provenance settles intent — and the FIRST write-up of it was wrong, corrected here after an adversarial audit.** It is a half-finished conversion, but not by the commit originally named. Verified against git: the census was per-rank from the start (before `c5823bb` it divided the whole layer once at the end, `params_per_layer_per_rank = transformer_param_layer / tp`); `173a765` ("MoE overhaul", 2026-01-06) made only the EXPERTS per-rank and *wrote* the `ep_dense_sync_bytes_dense` block with attention AND MLP undivided; the half-finished conversion is **`c5823bb` ("MLA PT#1 (Megatron-equal)", 2026-04-03)**, which introduced `_attention_param_components_per_rank` (`git log -S` returns exactly that commit and the fix), swapped it into the gradient helpers, and left the dense MLP at `ffn1_dim * d`. The commit that left the MLP behind is the one explicitly aiming at Megatron-equal accounting — about as clear an oversight signature as provenance gets. Megatron shards FFN1 column-parallel and FFN2 row-parallel; each rank owns `1/tp` of both and all-reduces only its own shard over the dp group. **Magnitude:** on a standard `4h` MLP a layer is `12h²`, of which `8h²` is MLP, so at `tp=8` the as-coded per-rank count is `4h²/8 + 8h² = 8.5h²` against the correct `1.5h²` — **5.667×** on BOTH the apply-grad price and the dp gradient payload. Measured on `GPT 22B, tp=8`: layer apply-grad `9.0476 ms → 1.597 ms`; the fused stage optimizer `434.284 ms → 76.654 ms`, i.e. **31.0 % → 5.5 %** of a 1.4 s measured step; dp reduce bytes per layer `641.73 MB → 113.25 MB`. **Fix:** one `_dense_ffn_param_components_per_rank` helper, used by all three call sites, so they cannot drift apart again. **T5 (direct `calc_time_llm`):** `GPT 22B pp1 full` **+26.57 % → +1.03 %**, `GPT 22B pp1 selective` **+11.30 % → -21.22 %** (that row is chronically under-predicted for reasons unrelated to the optimizer — it was `-27.27 %` before 10b too), running avg abs error **18.93 % → 11.12 %**. Pinned by `tests/test_grad_param_census.py` (per-rank identity with `_param_stats_per_rank`, aggregate dp-byte conservation across `tp ∈ {1,2,4,8}`, and bytes/price describing the same parameter set). |
| **11** ✅ FIXED (2026-07-27), **first attempt was defective — caught by adversarial audit** | **Nothing ordered the optimizer after its stage's GRADIENT REDUCER.** Re-measured before the fix: **every** dp collective was a graph SINK — 6/6 on `dp2tp1cp1pp2` at PIPELINE and FLAT, 24/24 on `dp2tp2cp2pp2` FLAT, 6/11 on zero2 — and 0 reducer→optimizer paths existed anywhere in the matrix. AstraSim gives each rank one COMP slot and one COMM slot, so a sink reducer means the weight update issued CONCURRENTLY with the all-reduce producing the gradients it applies. It is also why 10b's correction was invisible on all 14 `dp=2` rows. | **Fixed by a PAIR of rules, and either alone is wrong.** **R5b**: `OPTIMIZER(stage s)` depends on every gradient REDUCER of stage `s` on its own device. **R5c**: every collective that broadcasts the UPDATED parameters depends on the optimizer. *The first attempt shipped R5b only, and selected its sources by `key.phase is SyncPhase.GRAD`.* ZeRO-2's post-reduce parameter all-gather is declared in that same phase (`ZeRO2.after_reducer`), so it became a source and the optimizer was ordered `reduce-scatter → all-gather → update` — the physical inverse of `reduce-scatter → update → all-gather`. R5b's own docstring asserted the opposite in so many words. **A `SyncPhase` says WHERE in the schedule a requirement sits; it can never answer WHAT it is** — asking a position a role question is how the wrong edge got in. Roles are now DECLARED by the policy that knows: `SyncRequirement.is_reducer` / `.after_update`. **R5b's source set is every GRADIENT REDUCER, and they live in TWO policy modules** — rows **S2/S4/S9** (the dp reducers, `policies/sharding.py::DDP.requirements`) and rows **S12** (the EP gradient syncs, `policies/routing.py`). Missing the second is not hypothetical: the first role-based commit set the flag in `sharding.py` only, and the phase filter it replaced had been picking S12 up for free. Measured on committed HEAD, `dp2 ep2 moe pp2` FLAT: **16 `ep_sync` collectives became graph SINKS, 0 edges to the optimizer**, against 16 edges before — item 11's own bug, silently reintroduced on the ep axis. **It is NOT free, and a `total_time`-only diff says it is:** on `train:flattened:dp2tp2cp1pp2mb2sp1:moe:ep2` the aggregate is unchanged (the max-rank was already the bottleneck) while PER-RANK seconds move **+9.5 % on ranks 0-1** (0.08491143 -> 0.09295319) and +1.0 % on ranks 4-5. Only the T2 per-rank tier catches it; a `total_time` comparison — and the audit's own probe — read 0.00 s. That is a lesson about the diff, not about the bug. A whitelist maintained by hand is the shape of bug this entry exists to remove, so the PROPERTY is now asserted directly by `tests/test_build.py::test_every_gradient_reducer_reaches_its_optimizer` over dp / ZeRO-2 / EP configurations (verified to fail with the S12 flag removed, naming `transformer_dense_ep_sync_grad_b1_l3`). **Severity at production `ep` is still unbounded above:** these goldens keep the EP sync off the critical rank. Settling experiment: scale `moe_intermediate_size` (or `ep`) until `transformer_moe_ep_sync` is the max-rank tail, then A/B the flag. **Why the pair:** on a one-COMM-slot model `RS → gather → update` and `RS → update → gather` cost the SAME, so removing only the bad edge leaves the gather a SINK and it overlaps the update for free — cheaper than the mis-ordering it replaced. Measured on `flattened:…zero2`: inverted 0.09766584 s, sink-only 0.09293931 s, correct 0.09455452 s. **Deltas:** the two zero2 rows are the only ones R5b's role fix touches (analytical −11.17 % → +3.17 %, flattened −4.84 % → −3.19 % once R5c lands). Stage identity comes from `schedule.stage_of`, never a name. `meta.misc["r5_edges"]` records the post-R3 edge set so **D1** stays exactly re-derivable. Pinned by `tests/test_build.py::test_r5b_selects_reducers_by_role_not_by_schedule_phase`, verified to FAIL with the phase filter restored (it names `zero2_softmax_gather_grad_b1` feeding the optimizer). |
| **12** ✅ FIXED (2026-07-27) | **The fused optimizer node priced TRANSFORMER LAYERS ONLY — the embedding and the vocab projection got no apply-grad time at all**, while their gradients WERE all-reduced (`comm["embedding"]`/`comm["softmax"]`). The model reduced gradients it never applied. | **Fixed at `/tp`, and that is not a coin flip:** `_param_stats_per_rank` computes `embedding_params = vocab_size * hidden_dim / tp` and `output_params = 0 if tied else embedding_params` (`train_timing.py:813-815`). (An earlier draft also cited the embedding backward at `train_timing.py:2915`; that citation is **withdrawn** — it cuts both ways, since `_grad_accum_time` returns 0.0 whenever `gradient_accumulation_steps <= 1` (every T5 row) and the dominant term four lines earlier reads the table UNSHARDED.) New `get_embedding_apply_grad_llm` / `get_softmax_apply_grad_llm` reuse a shared `_embedding_softmax_params_per_rank` that mirrors those expressions verbatim — ViT branch, `tied_embeddings`, `disable_embedding_unembedding` included — so the price and the census cannot disagree. `optimizer_duration` adds them ONCE, on stage 0 and stage `pp-1` respectively (both on the single stage when `pp == 1`); a producer that never registers them contributes 0.0 and the function degrades to 10b behavior. **Why `/tp` and not unsharded** — the argument first given here ("10d is about PLACEMENT, an independent leg") is true but weak; the load-bearing one is structural: `LEGACY_PLACEMENT` places OPTIMIZER `PER_CLUSTER_RANK` (`program/placement.py`), and `optimizer_duration` has no device, so an unsharded `V·h` would be charged on all `tp` cluster ranks — `tp`× the aggregate, i.e. exactly the over-count 10d refuses. **The T5 metric cannot arbitrate this and was not used to**: measured A/B on the 2-row GPT 22B pair gives `/tp` 11.1007 %, unsharded 10.9495 %, no-term-at-all 11.1223 % — but the same harness has an INTERIOR MINIMUM near **64×** the committed endpoint parameter count — sweeping a scale factor `k` gives `k=0` 10.81 %, `k=1` (as committed) 11.17 %, `k=8` (the unsharded reading) 11.07 %, **`k=64` 9.81 %**, `k=128` 12.99 %. A metric minimized by charging 64× the real parameters cannot arbitrate between 1× and 8×. (The `k=0/1/8` end of that sweep is within the harness's 2-decimal print rounding and is not load-bearing; `k=64`/`k=128` are an order of magnitude outside it. The higher-precision A/B in the row above — `/tp` 11.1007 %, unsharded 10.9495 %, no term 11.1223 % — agrees that the three are indistinguishable.) Average absolute error is monotone in fictitious optimizer time on that pair, so it is degenerate here. On the unconfounded full-recompute row `/tp` wins outright (+1.19 % vs +2.30 %). What 10d does still cover is **15**. Pinned by `tests/test_policies.py::test_optimizer_duration_charges_the_endpoint_parameters_once` and `tests/test_grad_param_census.py`. |
| 10b ✅ ANSWERED + FIXED (2026-07-27) | **Optimizer apply-grad modeled once per pipeline *stage*, but priced at ONE layer's parameters.** `comp_times["optimizer"] = get_data_parallel_reduction_llm(...)` is one layer's params (`train_timing.py:2949-2971`), emitted once per stage — under-counted weight-update compute by `layers_on_stage` (`ceil(L/pp)` on the head stages). | **Owner decision: the GRAPH STRUCTURE is right and stays** — one fused optimizer node per stage is how real frameworks fuse the weight update. Only the DURATION is fixed. `program.work.optimizer_duration` sums the apply-grad price of every layer **that stage owns**, each at its own kind (`durations["optimizer"]` dense, new `durations["optimizer_moe"]` for MoE), reading the authoritative `LayerAssignment` from `BlockExpander` so the **remainder-first, uneven** split (`L=9,pp=4 -> (3,2,2,2)`) is honored per stage — no global scalar is correct for all stages. `apply_grad` is linear in `num_params`, so summing per-layer prices is exactly the fused cost of the stage's whole parameter set. **Measured:** all 7 inference rows bit-identical; per-bundle `compute_micros_total` +9.9 % … +29.2 % on every training row; `total_time` moves on **23 of 37** training rows, +5.3 % … +18.1 % (largest: `mesh2d` +18.10 %, `hybrid:…moe:ep2` +18.10 %, `flattened:…moe:ep2` +17.02 %); the other 14 are unchanged because a dp-collective tail already hides the optimizer (every `dp=2` / zero2 / zero3 / ga2 row). Memory peaks unchanged everywhere. Pinned by `tests/test_policies.py::test_optimizer_duration_scales_with_the_layers_its_stage_owns` / `…prices_each_layer_with_its_own_kind` / `test_stage_layers_follows_the_supplied_layer_assignment` and `tests/test_placement.py::test_optimizer_node_is_priced_for_every_layer_its_stage_owns`. **T5 (measured Megatron A100_korthi, 4 rows): avg abs error 11.13 % → 12.57 %, threshold 20 %, still green — MIXED**, see 10b(T5) below. **Final state (2026-07-27):** unchanged by the later 10c fix — they touch disjoint terms (10b the OPTIMIZER duration, 10c the `cross_layer` p2p payload). The two `flattened … moe:ep2` rows now carry both corrections; every other row carries only 10b's. |
| 10b(T5) ✅ ANSWERED (2026-07-27) — the cause was **13**, not the cost model | **The fix made agreement with measured hardware better on 2 rows and worse on 2, which pointed at a compensating error.** `GPT 22B bs4 pp1 selective` **-27.27 % → +10.91 %**, `GPT 175B pp8 selective` **-6.20 % → -3.10 %**, `GPT 175B pp8 full` **+7.46 % → +9.82 %**, `GPT 22B bs4 pp1 full` **-3.57 % → +26.43 %**. GPT 22B is `pp=1, L=48`, the largest possible scaling, and it was the row that overshot. | **The entry's own suspicion was wrong and backwards.** It guessed `apply_grad` charges ~2 optimizer passes because of the separate `grad_clipping` sweep. Measured on `A100_korthi`: the clip term is **0.8442 µs of a 399.2401 µs** apply-grad call = **0.2115 %**, because `grad_clipping` is the only full-parameter sweep that omits `mem_level=num_levels-1`, so it is priced against `mem_layer[0]` (registers) and lands compute-bound — see **16**, whose correction would ADD ~33 %. Nor are the bytes inflated: at this config `read 14 + write 10 = 24 B/param` (bf16 params, fp32 grads, fp32 Adam moments, no fp32 master copy), i.e. **less** than a real Megatron bf16 step's ~30. The actual compensating error is **13**, the un-sharded dense MLP: at `tp=8` a layer's apply-grad and its dp gradient payload were **5.667×** too large, and 10b's `L`× scaling is what turned that from invisible into a 30-point swing. Fixing 13 moves the same 4 rows to `+1.03 %`, `-21.22 %`, … (see 13). Do **not** revert 10b: the parameters a stage owns must be updated. |
| 10c (ep half) ✅ ANSWERED + FIXED (2026-07-27) | **The `/ep` in the cross-layer byte divisor is a DOUBLE DIVISION, not a sharding model.** `cross_layer` raw bytes are `precision.activations · micro_batch · hidden · seq` (`train_timing.py:2893-2902`, via `:4589`), and `micro_batch` is ALREADY `batch/(dp·ep·mb)` because `base_timing.py:490-495` sets `dp_dense = dp·ep` under MoE. So the raw value is **one EP owner's own microbatch**. `build.py::_cross_layer_size` then divided it again by `Placement.cluster_size() = tp·cp·ep`. Provenance: legacy `git show 85894c6:llm_execution.py:822` used `ceil(total/par_degree)` with `par_degree = tp·cp·ep` (`:369`) — a placement DEVICE count reused as a byte-SHARDING degree. **The ledger's old wording ("under EP it is not expert-sharded, so `/ep` under-counts") reached the right verdict from the wrong premise:** it reads as model (B) (EP ranks share tokens). The codebase is unambiguously model (A) — EP ranks own DISTINCT tokens — and that is *why* `/ep` is wrong. | **Fixed:** new `Placement.activation_shard_size()` (`tp·cp` at FLAT/BLOCK, `1` at PIPELINE) is what `_cross_layer_size` divides by. `cluster_size()` is UNTOUCHED — it correctly counts `tp·cp·ep` devices per stage, and the stage still emits one transfer per device, so `ep` now multiplies the transfer COUNT rather than shrinking the payload. **Decisive evidence (own measurement, `tp=cp=1`, so no tp/cp confound): conservation.** At a fixed global batch of 16, `ep 1→2→4` moved the per-dp-replica boundary aggregate `16,777,216 → 8,388,608 → 4,194,304` B — the same tokens crossing the same boundary, so it must be invariant; it scaled exactly `1/ep`. After the fix it is `16,777,216` on all three. **Corroboration:** (a) `memory_estimation`'s per-device residual for the SAME tensor is `raw/(tp·cp)` at every `ep`, so the model said a device *holds* `raw/(tp·cp)` while *shipping* `raw/(tp·cp·ep)` — one tensor, two answers, differing by exactly `ep`; after the fix the ratio is 1.000 on every `ep>1` shape (`ep=2 tp=2 sp1`, `ep=2 tp=1`, `ep=4 tp=1`). (b) A fixed-per-rank-microbatch control (`ep=1,gbs=8` vs `ep=2,gbs=16`, both `micro_batch=2`, identical raw value, identical per-device residual) still halved the emitted payload — a pure divisor artifact with nothing about the rank's workload changed. (c) Four independent per-rank quantities agree on model (A): the qkv GEMM M-dim, the MoE all-to-all owner tokens, the memory residual, and `train_timing._moe_local_share_divisor`'s own docstring ("in training every EP rank owns a distinct microbatch"); the cross-layer divisor was the sole dissenter. **No deeper inconsistency:** GEMM shapes and comm sizes do NOT assume different EP models — they both assume (A). **Delta:** 2 of 45 goldens move (`train:flattened:dp1tp2cp1pp2mb2sp1:moe:ep2` `total_time` +0.582 %, `train:flattened:dp2tp2cp1pp2mb2sp1:moe:ep2` +0.034 %); per-rank p2p payload ×2, `bytes_by_axis[p2p]` ×2.0000, op counts / COLL / COMP / all memory peaks bit-identical. The 2 PIPELINE-emitted MoE rows (`hybrid`, `hierarchical`) do not move: `cluster_size() == activation_shard_size() == 1` there. Pinned by `tests/test_build.py::test_r2_does_not_divide_cross_layer_bytes_by_ep` (ep 1/2/4) and `tests/test_placement.py::test_activation_shard_size_excludes_ep` (all 9 grids). **T5:** the only suite that exercises `ep>1` is `validation_scripts/moe_ep_validation.py`, which **cannot run in this environment** (pre-existing AstraSim topology assertion, identical with and without the fix — verified by A/B). Bounded instead from its own generated case configs: on a GPipe critical path (`2·(pp-1+mb-1)` un-overlapped hops) the pp p2p is **0.01 %–0.27 %** of the measured iteration time before and **0.04 %–1.06 %** after, i.e. **at most +0.79 pp** of step time on any row — far under the suite's 7.06 % MAPE and its ±20 % threshold. That suite is **non-discriminating** here; nothing in it defends the `/ep`. |
| 10c (tp/cp half) ✅ ANSWERED — resolved **CORRECT**, code unchanged (2026-07-27) | **`/tp` and `/cp` on the cross-layer bytes are right, including under plain TP.** The old wording ("under plain TP the boundary activation is *replicated*, so real aggregate is `tp`×") mis-identifies the tensor. | The pipeline-boundary tensor is the **residual stream** `(batch, seq, hidden)` — NOT head-sharded; head sharding applies to the intra-layer Q/K/V and the pre-output-projection tensors, which never cross a stage boundary. Under `tp_sp` and under `cp` it is genuinely **sequence-sharded**, so `/tp`, `/cp` are exact. Under **plain TP** (`tp_sp=0`) the device does hold a replicated copy — but Megatron's default `--scatter-gather-tensors-in-pipeline` splits it across the TP group for the p2p send precisely so the boundary is not sent `tp`×, so the emitted per-rank payload `raw/tp` is right and the aggregate is conserved. Measured: aggregate boundary bytes per dp replica are invariant across `tp2sp1`, `tp2sp0`, `cp2`, `tp2cp2sp1` and `tp4sp0` at a fixed global batch (8,388,608 / 16,777,216 B respectively) — only the memory model's per-device *residency* differs, which is correct (a replicated tensor is resident whole and sent split). **Golden coverage of `tp>1, tp_sp=0`:** 6 specs, of which **4 have `pp>1`** and therefore exercise a cross-layer transfer at all — `train:flattened:dp1tp2cp1pp2mb2sp0:fault_tp`, `train:hierarchical:dp1tp2cp1pp2mb2sp0:fault_tp`, `train:hierarchical:dp1tp2cp1pp2mb2sp0:fault_pp`, `inf:hierarchical:dp1tp2cp1pp2mb2sp0:vit`. Only the **first** is FLAT-emitted, so exactly ONE golden pins the plain-TP divisor; the other three are PIPELINE (`cluster_size()==1`, no division). The two `tp=4 sp0` `mesh2d` rows are `pp=1` and have no `cross_layer` key at all. A future reader wanting more coverage should add a `flattened, pp>1, tp=4, tp_sp=0` row. **Separate observation, NOT 10c and NOT fixed:** at `tp>1 AND cp>1` the transfer divides by `tp·cp` (conserved, correct) while the memory model shards the residual by only `max(cp)` — `train_timing._sequence_parallel_degree()` returns `cp` when `cp>1`, never `tp·cp` (`train_timing.py:335-346`). On `tp2cp2sp1` the two paths disagree by `tp`; the *transfer* side is the one that conserves, so this is a memory-model question. No golden's memory peak is affected by the transfer path, so nothing here changes; worth its own ledger item if anyone works the SP+CP memory model. |
| 1(ep) | ZeRO-3 gather over the `ep` axis: `_param_stats_per_rank` never divides by `ep`, so each of `ep` ranks gathers un-sharded expert params (over-count `ep`×). | Only settleable once flattened MoE is enabled (`llm_execution.py:697`). Unreachable, not wrong, today. |
| A2 ✅ ANSWERED (final wave) | Whether rank-0-only DP attach matters in practice at studied scales. | It matters, and the ledger's "contention, not critical path" framing is confirmed. At `par_degree = 4`: the **busiest** dp communicator's byte load is UNCHANGED (1.518 GB on `(0,8)` before and after) — what changes is that 8 dp communicators are busy instead of 2, and they share one AstraSim network dimension, so the dp phase stretches. Per-rank wall seconds rise on **all 16 ranks**, +158.7 % to +244.1 % (`train:flattened:dp2tp2cp2pp2mb2sp1`), and per-rank COLL bytes on the 12 formerly-idle ranks go 50,331,648 → 809,500,672 (×16.08) while ranks 0/4/8/12 do not move at all. Under the old model the dp dimension carried `1/par_degree` of its traffic. |
| **14** ✅ FIXED (2026-07-27) — with **11**, not after it | **ZeRO-2's post-reduce parameter all-gather was PARALLEL to the optimizer, not after it.** The real sequence is `reduce-scatter grads → each rank updates its 1/dp shard → all-gather params`; the model attached the gather `AFTER(reducer)` only. | Filed as "later" and then promoted, because an audit showed it is not separable from **11**: on a one-COMM-slot model the inverted `RS → gather → update` and the correct `RS → update → gather` cost the SAME, so fixing 11 alone (gather excluded from R5b's sources, left a SINK) UNDER-predicts instead of over-predicting. **R5c** wires `optimizer → gather` from a declared `SyncRequirement.after_update`, so the pair lands together. Applies to ZeRO-3 too, which inherits `after_reducer`. The `SyncAnchor` vocabulary gap noted in the original entry is sidestepped: R5c runs in `build.py`, where the optimizer chain is already resolved, rather than needing an anchor the policy cannot name. Pinned by the same `test_r5b_selects_reducers_by_role_not_by_schedule_phase`, which asserts BOTH that the gather does not feed the optimizer AND that it is not a sink. |
| **15** ⚠ NEW (2026-07-27, found while fixing 12) | **`embedding_size` / `softmax_size` — the dp gradient BYTES for the endpoint tables — are still not per-rank.** `train_timing.py:4587-4589` computes `grad_comm·(vocab·hidden + seq·hidden·batch)` and `grad_comm·hidden·vocab` with no `/tp`, while `_param_stats_per_rank` (and now item 12's apply-grad term) says a rank owns `vocab·hidden/tp`. This is the LAST disagreement with the per-rank census; items 12 and 13 removed the others. **It is not only about `/tp` — the byte leg is also TIED-UNAWARE.** Every golden runs `tied_embeddings=True`, where the vocab projection IS the embedding table. Item 12's price leg handles that correctly (softmax apply-grad is 0.0 s when tied, verified in all four `(tp, tied)` cells), but `softmax_size` still all-reduces a full `hidden x vocab` payload — measured at `tp=2`: price 0, bytes 131,072 params-equivalent; and the embedding line reduces 262,144 params-equivalent against a price of 65,536. So the fix must make the bytes both per-rank AND tied-aware, in one commit. | Same class as 13, but entangled with class-B **10d**, which argued the unsharded bytes are consistent with modeling the softmax GEMM unsharded on one device. That argument covers the COMPUTE leg; the byte leg has an independent, unambiguous answer. **Also visible here:** the `+ seq_len·hidden·batch` term inside `embedding_size` is activation-shaped, not parameter-shaped — learned position embeddings are `seq_len·hidden` with no batch factor. Both belong in one commit with a fresh look at 10d. **Expected delta:** dp-collective bytes fall by ~`tp` on the two endpoint reducers; `dp>1` totals decrease. |
| **16** ✅ NEW + FIXED (2026-07-27) | **`grad_clipping` was priced against the REGISTER FILE.** It was the only full-parameter sweep calling `roofline(...)` without `mem_level`, and a scalar `mem_access` put it in single-level mode at `mem_layer[0]`. Measured: **0.8442 us of a 399.2401 us apply-grad call = 0.2115 %**. | **The mechanism was right and the physics label was wrong** (caught by adversarial audit; the ledger first said "compute-bound at peak FLOPs"). At L0 it lands MEMORY-bound: `comp_int = 4n/(2*grad_bytes*n) = 0.5` against an L0 inflection `th/bw0 = 3.1187e14/1.3414e14 = 2.3249`, so `roofline` takes the memory branch and charges 8 B/param at ~134 TB/s. The measured 0.8442 us equals `8N/1.3414e14` exactly; a genuinely compute-bound term would have been `4N/th` = 0.1816 us, 4.65x smaller — i.e. the ledger's own number disproved its own label. **Fixed:** `grad_clipping` now charges at `num_levels-1` (DRAM), which is where a gradient buffer lives. Both terms are then memory-bound at the same bandwidth, so it adds exactly `2*precision.gradients/(read+write per param)` = **+33.3 %** at `a100_80GB_korthikanti` (0.2115 % is config-specific). **`roofline` now REQUIRES `mem_level` for a scalar** — which surfaced five callers reaching level 0 by omission, three of them inside the analytical collective model. See **16b**/**16c**. |
| **17** ⚠ NEW (2026-07-27) | **Should apply-grad be `/dp` at `zero_stage == 1`?** `get_data_parallel_reduction_llm` divides only when `zero_stage >= 2`. Classic ZeRO-1 shards optimizer STATE and therefore the update: each rank applies `1/dp` of the parameters and all-gathers the result. Megatron's `--use-distributed-optimizer` is exactly that, and it is on by default in the runs the T5 suite validates against. | If ZeRO-1 means what it means everywhere else, the divisor belongs at `>= 1`; the current code charges every rank the full update at stage 1. `program/policies/sharding.py::ZeRO1` already documents itself as "structurally identical to DDP — shards optimizer state only, which changes MEMORY, not the collective set", so the two halves of the codebase disagree about whether ZeRO-1 touches compute. **Zero effect on the A100_korthi T5 rows** (all `dp=1`) and on the golden matrix (no `zero_stage == 1` spec), so this needs a config to exercise it before it can be settled. |
| **16b** ⚠ NEW (2026-07-27) | **`_gemm_pointwise_time` is priced at `mem_layer[0]` too.** Same omission as 16; now PINNED explicitly at `mem_level=0` (`base_timing.py`) so behavior is unchanged and the choice is visible. | A GEMM epilogue plausibly does run out of fast memory while the output tile is hot, which is why this was not moved to DRAM alongside `grad_clipping`. But `point_mem` scales with the FULL `m*n` output, which for a large GEMM cannot be register-resident. Settling it moves **every GEMM in the model**, so it needs its own review and delta table. |
| **16c** ⚠ NEW (2026-07-27) | **The analytical collective model prices its per-hop buffer traffic at `mem_layer[0]`.** Five `_roofline` calls in `_analytical_all_reduce` / `_analytical_reduce_scatter` / `_analytical_all_gather` reached level 0 by omission; now PINNED at `mem_level=0`, behavior unchanged. | A ring hop's chunk is `size/participants`, which for a gradient buffer is HBM-resident, so DRAM is the likely right answer. This term is inside **every analytical collective**, so it needs its own review. Found only because requiring `mem_level` turned the implicit default into a hard error. |
| **18** ✅ NEW + FIXED (2026-07-27) | **Four independent answers to "how many parameters does this rank own".** `_param_stats_per_rank` (memory/ZeRO), the apply-grad price, the dp gradient payload and the EP-sync payload each computed it, and three disagreed until items 12 and 13. The MoE expert block existed VERBATIM twice, with a partial third copy. | **Consolidated into one census:** `layer_params_per_rank(hidden, intermediate, moe=)` returns per-component per-rank counts, built from `_attention_param_components_per_rank`, `_dense_ffn_param_components_per_rank` and the new `_moe_ffn_param_components_per_rank`; `_embedding_softmax_params_per_rank` covers the endpoints. All four sites read it. **Behavior-preserving** — the whole matrix is unchanged by this commit's census half (the movement in the same commit is items 16 and the R5b role fix). Gated by `tests/test_grad_param_census.py::test_every_census_reader_agrees_dense` / `..._moe` (which also pins the `tp_ep` switch) and `test_census_is_the_only_place_the_mlp_divisor_is_written`, a source-level check that no reader re-derives `ffn_proj_factor` / `_ffn1_output_dim` / `expert_param_size` / `vocab_size *`. |
| **19** ⚠ NEW (2026-07-27) | **There is no `cp`-axis gradient reduction.** Every dp collective carries `axes=('dp',)` and `participants=dp` (measured on `dp2tp2cp2pp2`), yet CP ranks hold REPLICATED parameters and compute PARTIAL gradients from different sequence chunks. Megatron reduces gradients over the DP×CP group. | Two consequences. (a) Correctness: partial gradients are never summed across `cp`. (b) Volume: with the reducer emitted per cluster rank (**A2**), aggregate dp traffic per replica is `cp*ep*P` where the distinct parameter volume is `P` — measured **2x** at `cp=2` (2,516,582,400 B against 1,258,291,200 B of distinct layer gradient), while `tp=1` conserves exactly. Pre-existing; NOT introduced by item 13, whose per-rank payload is exactly the census value at every `tp`. Fix shape: make the reducer's group `dp x cp` (participants and members), or add a `cp` reduction. `ep` is a separate question — EP ranks own DISTINCT experts, so the over-count applies only to their replicated attention/router params (ledger **1(ep)**). |
| **20** ⚠ NEW (2026-07-27, from the 16 audit) | **The memory hierarchy is not monotonically ordered by bandwidth in 5 shipped configs.** `H100_SXM5_80GB_case-{C,D,E,F,G}.yaml` put DRAM (L3) at 1.83e13 / 1.34e13 B/s against SRAM-L2 at 8.74e12 B/s, so `num_levels-1` is the LAST level but not the SLOWEST one. Measured across the 33 parseable hardware configs; the other 28 are monotone. | Nothing here is wrong today — `roofline`'s multi-level branch takes `max` over per-level times, which is correct either way, and item 16's error text says "`num_levels-1` = DRAM", not "= slowest". But any future code that reads level ORDER as a proxy for SPEED is wrong on these five. They are sensitivity-study configs, so the inversion may be deliberate (an L2 with low effective utilization); worth a comment in the config if so. **Also from the same sweep:** item 16's `0.2115 %` clip share is config-specific — it ranges **0.136 %** (`a100_80GB.yaml`) to **3.715 %** (`H100_SXM5_80GB_case-G`). |
